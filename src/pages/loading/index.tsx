import { Loading } from "../../components/loading";
import * as styled from "./styled";

export const LoadingPage = () => {
  return (
    <styled.Container>
      <Loading />
    </styled.Container>
  );
};
